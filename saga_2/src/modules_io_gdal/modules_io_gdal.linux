# Linux GCC Makefile
# Created by O. Conrad

export SAGA = /windows/data/develop/saga_2/src

PROJECTS = ./io_grid_gdal/io_grid_gdal

all:
	@for i in $(PROJECTS); do (cd $$i && $(MAKE) -fmakefile.linux); done

clean:
	@for i in $(PROJECTS); do (cd $$i && $(MAKE) -fmakefile.linux clean); done

.PHONY: all clean
